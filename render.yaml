# Render Blueprint — API service. `npm run build` skips Vite if devDependencies are omitted.

services:
  - type: web
    name: brokercrm-api
    runtime: node
    buildCommand: npm ci
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
